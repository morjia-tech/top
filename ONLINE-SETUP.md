# サイト内で編集・公開するための初回設定

**現在は接続済みです。既存サイトの更新では以下の初回設定・SQL実行を繰り返さないでください。** 新しい管理機能と画像登録の説明は `CHANGES.md` を参照してください。以下は新規環境を作る際の参考です。

この設定が済むと、サイト下部の「管理者ログイン」から、文章・メールアドレス・作品説明・URLを編集し、「保存して公開」で更新できます。毎回ファイルを書き出す必要はありません。

現在は接続前です。編集画面と接続処理は入っていますが、以下の設定と初回アップロードが済むまではオンライン保存できません。

## 1. Supabaseのプロジェクトを作る

https://supabase.com/dashboard を開き、アカウントを作ってログインします。New projectから、このサイト用のプロジェクトを作ります。プラン・利用条件を確認して選択してください。データベース用パスワードは自分で保管し、このサイトやチャットには貼り付けません。

## 2. 自分専用のログインを作る

プロジェクトのAuthentication → UsersでAdd user → Create new userを開き、自分のメールアドレスと新しいパスワードを指定します。Auto Confirm Userの項目があれば有効にします。これがサイト編集に使うログインです。パスワードはチャットに送らないでください。

作成したユーザーのUser UID（UUID形式）を控えます。一般の新規登録はAuthenticationの設定でAllow new users to sign upをOFFにします。メニュー名はSupabaseの画面変更で異なることがあります。

## 3. 自分だけが更新できる保存先を作る

同梱の `setup/supabase.sql` をテキストエディターで開きます。`YOUR_ADMIN_USER_UUID` を手順2のUIDに置き換え、プロジェクトのSQL Editorに全文を貼り付けてRunします。

このSQLは初回用です。成功後は繰り返し実行する必要はありません。エラーが出た場合はそのまま公開せず、エラー内容を確認してください。

テーブルは `portfolio_content`、行のIDは `main` です。閲覧は全員に許可、更新は指定したUIDだけに許可します。編集ボタンの表示ではなく、データベースのRow Level Security（RLS）で判定します。匿名や別のログインユーザーは更新できません。所有者の変更・行追加・削除もブラウザには許可しません。

## 4. 公開用の接続情報を設定する

プロジェクトのConnect、またはSettingsのAPI Keysから以下を確認し、`cloud-config.js` に入れます。

```js
window.MORJIA_CLOUD = {
  url: "https://自分のプロジェクトID.supabase.co",
  publishableKey: "sb_publishable_で始まる公開用キー",
  documentId: "main"
};
```

Publishable keyはブラウザで使う公開用の値です。**Secret key / service_role / データベースパスワードは絶対に入れないでください。** 管理者のログインパスワードはコードには書きません。

設定を手伝ってもらうときに必要なのはProject URL、Publishable key、管理者UIDです。ログインパスワードは自分で画面に入力します。

## 5. 動作確認して初回公開する

サイトを開いて、下部の「管理者ログイン」を押します。手順2のメールアドレスとパスワードでログインし、内容を確認して「保存して公開」を押します。最初の保存で、同梱されている既存の紹介文や作品も保存先に登録されます。

ログインできたら別のブラウザやシークレットウィンドウでもサイトを開き、保存した内容が見えることを確認します。匿名の閲覧者から保存できないことも確認してください。管理者以外のユーザーの書き込み拒否は、実接続後に確認する必要があります。

GitHub Pagesには更新済みのサイト一式を初回アップロードします。公開中のprofを残す場合は別リポジトリを使います。既存profへの上書きはまだ行っていません。初回以降、文章やURLの更新にはGitHubの操作は不要です。

## 普段の編集

1. サイト下部の「管理者ログイン」を押す。
2. 自分のメールアドレス・パスワードでログイン。
3. HOME、ABOUT、LINKS、CONTACT、WORKSの項目を開いて入力。
4. 「保存して公開」を押し、確認画面で確定する。
5. 「保存しました。公開内容に反映済みです。」が出たら完了。
6. 「閉じる」で編集を終了し、ログアウトする。

ほかの閲覧者にはページを開く・再読み込みした時点で最新内容が表示されます。開きっぱなしの他人のタブへ即時配信する方式ではありません。

未保存の内容はブラウザ再読み込みで失われます。接続・保存エラー時は編集欄を保持し、成功と表示しません。複数タブが同時に保存した場合は古い内容で上書きせず、競合メッセージを出します。資格情報はメモリーにだけ保持し、再読み込み後は再ログインします。

## 今回の編集対象

サイト名、HOMEの文章、ABOUTの自己紹介、SNSのURL、CONTACTのメール・フォームURL、登録済み作品の名前・説明・URLを編集できます。画像アップロード・作品追加削除・色変更は今回のオンライン編集の対象外です。

接続後の公開内容はSupabase側が優先されます。`site-data.js` は初期値と接続障害時の代替表示です。そこで作品追加などの構造変更を行う場合は、保存先の内容とも合わせる必要があります。日常の文章編集はサイト内から行ってください。障害時には古い同梱内容が表示される場合があり、その旨をページに表示します。

作品の簡易パスワード（auth.js）と管理者ログインは別です。作品の簡易パスワードは引き続き秘密を守るものではありません。今回の保存先に入れる情報はすべて公開用です。

公式資料:
- https://supabase.com/docs/guides/getting-started/api-keys
- https://supabase.com/docs/guides/database/postgres/row-level-security
