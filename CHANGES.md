# MORJIA /top/ 更新版

## 追加調整：丸いフォント・共通バナー・編集ツリー

- フォントをGoogle Fontsの「Zen Maru Gothic」に変更。通信できない場合は端末の代替フォントで表示します。
- HOMEの「WORKSを見る」を削除し、WORKS / GALLERY / ABOUT、LINKS / CONTACTの3列2段のリンクに変更しました。
- 全ページの上部にHOMEと共通の透過バナーを表示します。バナー画像は引き続き `home.banner` 一か所で変更できます。
- 編集画面は親のページ名と子の作品名・画像名・リンク名を、三角の開閉ボタンと枝線で表示します。初期状態は折りたたみ、追加した項目は自動で開きます。編集中のタイトル変更もツリーに反映されます。
- 主な変更: `index.html` / `styles.css` / `app.js` / `admin.js`。Supabaseの設定変更は不要です。

## 反映方法

このフォルダの中身を、現在の `morjia-tech/top` リポジトリの直下へ上書きします。リポジトリを作り直す必要はなく、公開URLは `https://morjia-tech.github.io/top/` のままです。ZIPのままアップロードせず解凍してください。公開サイトへのアップロードはまだ行っていません。

既存Supabaseの接続先・管理者・RLSをそのまま使います。**SQLの再実行は不要**です。保存済みのCONTACT、作品、リンクを読み込み、データ形式を表示時に変換します。勝手にサーバーへ保存はしません。管理画面で保存すると、新形式になります。更新後は古い編集タブを閉じてから編集してください。

## 使い方

- ナビゲーションはHOME / WORKS / GALLERY / ABOUT / LINKS / CONTACTです。
- 管理者ログインはページ最下部の控えめな「管理」リンク風ボタンです。本人確認・保存権限は以前と同じです。
- 編集画面でWORKS・GALLERY・LINKSを開き、「＋」で追加します。各項目の「↑ 上へ」「↓ 下へ」で順序変更、「削除」で除去します。
- 変更は下部の「保存して公開」で反映します。「閉じる」はその隣です。保存前の削除は公開サイトに影響しません。
- 保存エラー時は入力を残します。別タブによる更新との競合検出も維持しています。

## WORKSとTEST PLAY

カテゴリ: GAME / TOOL / APP / OTHER（管理画面では `TOOL / APP` が1つのカテゴリです）。ステータス: MAKING / TEST PLAY / RELEASED。どちらも未設定を選択できます。既存の「制作中」はMAKING、TOOLはTOOL / APPとして引き継ぎます。

作品ごとにタイトル、サムネイル、短い説明、詳細説明、リンクURL、TEST PLAY URL、ダウンロードURL、外部サイトURLなどを設定できます。空欄の項目は表示しません。IDは自動作成されます。公開後にIDを変更すると詳細ページのリンクが変わります。

GAMEかつTEST PLAYで、有効なTEST PLAY URLがある場合だけ一覧と詳細に遊ぶボタンが出ます。`#play/作品ID` でゲームをサイト内表示できます。実際のゲームは追加していません。

配置例: `works/my-game/game/index.html`。TEST PLAY URLも `works/my-game/game/index.html` と設定します。先頭に `/` を付けないでください。相対パスにより `/top/` 内で解決します。ゲームのファイル群はGitHubへ別途配置します。詳細な制限は `works/README.md` にあります。iframeを許可しない外部サイトや一部のゲームは「別画面で開く」を使用します。

## GALLERYと画像登録

画像、タイトル、短いコメント、自由入力のカテゴリを登録できます。サムネイルを押すと拡大表示し、「閉じる」またはEscで戻れます。画像が未設定の項目は公開一覧に表示しません。画像0件も対応しています。

WORKSのサムネイルとGALLERYの画像は、URL・相対パスの入力、またはPC/スマホのファイル選択で登録できます。PNG / JPEG / WebPは**1枚20MBまで**選択可能です。1MBを超える画像は長辺1600px以下・1MB以下のWebPに自動変換します（アニメーションPNG/WebPは静止画になります）。GIFはアニメーション保持のため1MB以下のみです。HEICはJPEG等に変換してください。

選択後は画像欄にプレビュー・ファイル名・結果が表示されます。「保存して公開」を押すまでは公開内容は変わりません。「画像を外す」で未設定にも戻せます。読み込み失敗時は元の画像を保持し、画像欄に理由を表示します。

画像データを公開コンテンツと一緒に保存するので、別のStorage設定は不要です。保存データ全体は約8MBを上限にしています。大量・高解像度の画像は外部画像URLやGitHubに配置した `assets/...` を指定してください。

## LINKS

名前・URL・任意の短い説明を1件ずつ管理します。以前のGitHub / itch.io / X / Steamと独自リンクは、従来の順番で新しい一覧に引き継がれます。以後は自由に並べ替えできます。

## 猫と背景

猫をマウスやスマホの指でドラッグして移動できます。ドラッグ中は自動行動を止め、離すと約3秒休んで再開します。タップ・クリック・キーボードのEnter/スペースで反応します。配置は閲覧中のみで、再読み込み後には引き継ぎません。

以前の半透明フレーム2枚の重ね描画をやめ、キャンバスを消去して1枚だけ不透明で描画します。移動位置は従来どおり画面更新に合わせて動きます。画像自体と各動作は再利用しています。

背景色はアイボリーのまま、ごく薄い斜め格子をCSSで加えています。CONTACTの表示・メールリンク・フォームURLは変更していません。

## 変更ファイル

| ファイル | 主な変更 |
| --- | --- |
| index.html | 新データ変換処理の読み込み、更新時のキャッシュ対策 |
| site-data.js | GALLERY、6メニュー、LINKS一覧の初期構造 |
| data-schema.js（新規） | 旧データの互換変換、追加項目の検証 |
| cloud.js | GALLERYと新LINKS形式の読み込み・保存 |
| app.js | GALLERY拡大表示、WORKS項目、TEST PLAY画面、新LINKS表示 |
| admin.js | 項目追加・削除・順序変更・画像選択、下部操作ボタン |
| morjia-pet.js | マウス・タッチのドラッグ、重ね描画の廃止 |
| styles.css | 薄い背景柄、ギャラリー・管理画面、目立たない管理入口 |
| README.md / ONLINE-SETUP.md / works/README.md / CHANGES.md | 更新内容と運用方法 |

認証設定・公開キー・Supabaseの権限・作品の簡易パスワード処理は変更していません。

## 検証

テスト用の保存先で、旧データの読み込み、追加・削除・並び替え後の保存と再読込、保存失敗時の入力保持、CONTACT不変、拡大表示、ゲーム用iframe、マウスとタッチでのドラッグ、画面幅375pxでのはみ出しを確認しています。テスト用の作品・リンク・画像は公開データに書き込んでいません。実際のゲームの互換性はゲーム追加時に確認が必要です。
